<UserControl x:Class="EventPass.EventControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EventPass"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">

    <Viewbox Stretch="Uniform">
        <Grid Width="400" Height="450">
            <Border BorderBrush="White" BorderThickness="1" CornerRadius="30" Background="White"/>
            <Grid Margin="10">
                <Image x:Name="Image_EventImage" Margin="21,23,25,168"/>
                <Label x:Name="Label_EventName" Content="Label" Margin="27,0,27,105" FontSize="22" FontWeight="Bold" Height="39" VerticalAlignment="Bottom"/>
                <Button x:Name="Button_View" Content="View" Margin="62,0,62,27" Background="#FF9B72CF" Foreground="White" FontSize="22" FontWeight="Bold" Height="59" VerticalAlignment="Bottom" Cursor="Hand" BorderBrush="{x:Null}" Click="Button_View_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="30">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  RecognizesAccessKey="True"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#decdff"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Grid>
    </Viewbox>
</UserControl>
